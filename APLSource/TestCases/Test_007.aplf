 R←Test_007(stopFlag batchFlag);⎕TRAP;data;more;rc
⍝ Get list of releases with the dedicated method `GetAllReleases`
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 (rc more data)←∆MyGitHubAPI.GetAllReleases'TestRepo'
 →PassesIf 0=rc
 →PassesIf 0=≢more
 →PassesIf(≢data.name)∊3 4  ⍝ Only users with "Push" rights will see the draft release!

 R←∆OK
⍝Done
